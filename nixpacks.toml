[phases.setup]
nixPkgs = ['python39', 'pip', 'gcc', 'glib', 'libGL']

[phases.install]
cmds = [
  'pip install --upgrade pip',
  'pip install --no-cache-dir -r requirements.txt'
]

[start]
cmd = 'gunicorn --bind 0.0.0.0:$PORT app:app --timeout 120 --workers 1'